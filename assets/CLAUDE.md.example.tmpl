# {{NAME}} — Claude Code notes

> **Per-X-Tension instructions for Claude Code.** Copy this file to `CLAUDE.md`
> (git-ignored) in this folder and tailor it for your environment. Claude reads
> `CLAUDE.md` as directory-level instructions. Keep this committed `.example`
> **generic — no local paths, credentials, or case data.**
>
> Project-wide rules live in your project's `CLAUDE.md` and the
> `xways-xtension-authoring` skill (installed as a Claude Code plugin, or a
> clone/junction). This file captures only what is specific to **{{NAME}}**.

## What this X-Tension is

{{DESCRIPTION}}

- **Stem / DLL:** `{{NAME}}` → `{{NAME}}.dll`
- **Version:** `{{VERSION}}` — beta until the first public release (see
  the skill's `docs/conventions/versioning.md`).
- **Identity constants** (top of `{{NAME}}.cpp`): `NAME`, `VERSION`,
  `DESCRIPTION`, `REPORT_TABLE`.

## Build

```powershell
# Run from your project root; the script lives in the skill (plugin cache or clone).
<skill>/scripts/build-xtension.ps1 -Name {{NAME}}
```

Close X-Ways first — the DLL is locked while it runs (no hot reload).

## Wrapped tool / dependencies

- _If this wraps an external CLI: name it, its `--version` identity needle, and
  the exact argument shape used. See
  the skill's `docs/conventions/helper-exe-verification.md`._

## Config & sidecar files

- _List cfg keys and any sidecar files that live next to the DLL. Live `.cfg`
  files are git-ignored; only `.cfg.example` is committed._

## Gotchas / notes

- _Anything non-obvious about this X-Tension's behavior, quirks, or test data._

## Conventions in play

- Output directory → the skill's `docs/conventions/output-dir.md`
- Verbose logging → the skill's `docs/conventions/verbose-logging.md`
- _Add others as wired (ctrl-to-save, manager-compatibility, …)._

## Don't commit

- Compiled `{{NAME}}.dll`, live `.cfg`, local paths (`C:\Users\…`), or case
  data. See the skill's `docs/conventions/repo-hygiene.md`.
